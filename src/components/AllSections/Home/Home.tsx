import { Stack } from "@mui/material";

function Home() {
  return (
    <Stack
      sx={{
        alignItems: "center",
        justifyContent: "center",
        color: "white",
        marginTop: "auto",
      }}
    >
      ByteUI-Core
    </Stack>
  );
}

export default Home;
