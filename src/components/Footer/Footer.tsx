import { Stack } from "@mui/material";

function Footer() {
  return (
    <Stack
      sx={{
        alignItems: "center",
        color: "#ffff",
        padding: "1rem",
        marginTop: "auto",
      }}
    >
      @Surjendu kar
    </Stack>
  );
}

export default Footer;
